{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Preview build",
            "request": "launch",
            "command": "npm run build\nnpm run preview",
            "cwd": "${workspaceFolder}"
        }
    ]
}